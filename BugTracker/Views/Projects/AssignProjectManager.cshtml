@model BugTracker.Models.ViewModels.AssignPMViewModel

@{
    ViewData["Title"] = "Assign PM";
}
<h1>Project : @Model.Project?.Name</h1>
<h4>Assign Project Manager</h4>
<hr />
<div class="row card">
    <div class="col-md-4">
        <form method="post" asp-action="AssignProjectManager" asp-controller="Projects">
            <input type="hidden" asp-for="Project!.Id" />

            <h2>@Model.Project?.Name </h2>

            <p>@Model.Project?.Description </p>

            <div class="form-group">
                <label class="control-label">Select Project Manager</label>
                <select asp-for="PMID" class="form-control text-black" asp-items="@Model.PMList">
                    <option>Choose a PM</option>
                </select>
                <span asp-validation-for="PMID" class="text-danger"></span>
            </div>
            <input type="submit" value="Assign" class="btn btn-primary" />
        </form>
    </div>
</div>
<div>
    <a asp-action="UnassignedProjects">Back to List</a>
</div>
